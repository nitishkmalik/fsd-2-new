In this experiment we have implemented routing in single page application using react that works without reloding pages . In this we have created new folder using 'npm create vite@latest Exp1' we created exp 3 and in this we select react framework and javascript and installed it with npm(node package manager) and used npm run dev to run our web page. Inside exp1 in src folder we have made a folder component in src and made 3 files about.jsx , home.jsx , spa.jsx and made diffeent components and imported app and home.jsx in spa.jsx. whereas we used ' npm install react-router-dom' to move between pages in a React app without reloading the page. after that we pushed it into git and deployed dist into netlify manually . In static web page it shows same content does not change whereas in dynamic web page conteny changes and responds to user. Routing in web pages is the mechanism that determines how an application responds to a specific user request—essentially, which content is displayed based on the URL the user visits (e.g., /home, /about, /products)
